RewriteEngine On

# Route all API requests to router
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

# Protect sensitive files
<FilesMatch "^(config)\.php$">
    Require all denied
</FilesMatch>
